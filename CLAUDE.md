# 입퇴실 도우미 — Claude 작업 지침

## 비즈니스 컨텍스트

### 제품 정의
임대인이 임차인의 입실·퇴실을 링크 하나로 처리하는 SaaS 도구.
임차인은 앱 설치 없이 링크로 참여하고, 임대인은 방 상태 기록·비밀번호 전달·보증금 정산까지 한 곳에서 관리한다.

### 핵심 가치 제안 (랜딩 페이지 스토리라인과 동일)
- **멀리서도 확인**: 퇴실 때 가보지 않아도 사진으로 방 상태를 확인하고 보증금을 돌려줄 수 있음
- **자연스러운 청소 유도**: 사진을 찍어 올려야 하니 임차인이 나가기 전에 정리하게 됨
- **체크리스트로 빠뜨림 방지**: 비밀번호·공과금·반환 계좌를 채워야 제출 가능
- **앱 설치 없음**: 임차인에게 링크만 보내면 끝

### 타깃 유저 (우선순위 순, 공유 탭 문구 기준)
1. 직장 다니면서 임대업 하시는 분
2. 퇴실 때마다 방을 확인하러 다니는 게 부담스러운 분
3. 단기임대·에어비앤비 운영하시는 분
4. 고시원·다가구 관리하시는 분
- **공통 페인포인트**: 퇴실 사진 수거 번거로움, 비밀번호 전달하는 불편함, 보증금 분쟁

### 성장 전략
- 무료로 시작 → 임차인 제출 완료 시 후기 수집(계정당 최초 1회만 유도) → 랜딩 사회적 증거로 전환
- 공유 탭으로 임대인 바이럴 유도
- 채널 탭 "임대인클래스 콘텐츠" 섹션 → 운영 중인 임대인클래스 브랜드와 연계

### 법적/개인정보 관련 결정사항 (2026-09-21 논의)
- 사업자등록증이 아직 없어 **카카오 알림톡은 도입 보류**. 링크 전달은 임대인이 문자/카톡으로 직접 복사-붙여넣기 하는 방식 유지 (앱이 대신 발송하지 않음)
- 임차인 전화번호는 메모에서 **선택 입력**
- 회원가입 시 개인정보 수집·이용 동의(필수) + 휴대폰번호 수집·이용 동의(선택) 필요 — 구현 완료
- 임차인이 보는 입실/퇴실 폼에 개인정보 안내 문구 필요 — 구현 완료 (정식 출시 전엔 체크박스형 명시적 동의로 강화 검토)
- 정식 개인정보처리방침 문서는 아직 없음 — Supabase 연결 전 준비 필요

---

## 앱 구조 (현재 상태 — 2026-09-21 기준)

### 기술 스택
- React 19 + Vite 8, 단일 파일 `src/App.jsx` (약 1980줄)
- 라우팅: React Router 없음. 자체 `push(view, ctx)` / `pop()` 스택 사용 (브라우저 히스토리와 동기화됨)
- 데이터: 전부 `useState` in-memory (새로고침하면 날아감 — Supabase 미연결, **다음 최우선 작업**)
- 스타일: 100% inline style (CSS 파일은 `index.css`에 리셋 + 랜딩 줄바꿈 규칙만)
- 배포: Vercel — https://ipteosil.vercel.app (GitHub `main` 브랜치 push 시 자동 배포, 약 20~30초 소요)
- 랜딩용 사진: `public/landing/` (실제 앱 캡처 + 사용자 제공 방 사진, 가벼운 jpg로 리사이즈됨). 원본은 `landing-assets/`에 있고 `.gitignore` 처리되어 저장소에는 올라가지 않음

### 화면/컴포넌트 목록 (2026-09-21 기준, PropDetailPage는 삭제됨)
| 화면 | 컴포넌트 | 진입 경로 |
|------|----------|-----------|
| 랜딩 | `LandingPage` | 비로그인 최초 방문 — "이렇게 사용해요" 사용설명서 구조 |
| 로그인/가입 | `AuthPage` | 랜딩 → "시작하기" — 휴대폰번호(선택) + 동의 항목 포함 |
| 홈 | `HomeTab` | 로그인 후 기본 탭 — 방 카드 안에 4단계 진행 상태(세로선) 표시, 길게 누르면 순서 변경 |
| 방 정보 수정 | `AddPropPage` | 홈 카드 ⋯메뉴 → "방 이름·주소 수정" (이름/주소만, 비밀번호·사진은 여기 없음) |
| 메모 (계약 정보) | `ContractMemoPage` | 홈 카드 "메모" 행 → 임차인 정보·계약조건·자유 메모장(크기 조절 가능) |
| 입실 링크 설정 | `CheckinSetupPage` | 홈 카드 "입실 링크" 행 → 비밀번호, 기준 사진, 보낼 메시지, 링크 복사 |
| 퇴실 링크 설정 | `CheckoutSetupPage` | 홈 카드 "퇴실 링크" 행 (입실 확인 후 잠금 해제) → 보낼 메시지, 링크 복사 |
| 계약 기록/최종 보고서 | `RecordPage` | 홈 카드 "최종 보고서" 행 (퇴실 제출 후) / 지난 보고서 목록에서도 진입 |
| 지난 보고서 목록 | `PastReportsPage` | 홈 카드 하단 "지난 보고서 N건" 링크 → 목록 + 개별 삭제 |
| 입실 폼 (임차인) | `CheckinForm` | `?token=...` URL |
| 퇴실 폼 (임차인) | `CheckoutForm` | `?token=...` URL |
| 채널 | `ChannelTab` | 하단 탭 |
| 공유 | `ShareTab` | 하단 탭 — 카카오톡/링크 복사, 추천 대상 5개 |
| 설정 | `SettingsTab` | 하단 탭 |
| 관리자 | `AdminTab` | 하단 탭 (isAdmin 전용) |

**삭제된 화면**: `PropDetailPage`(방 상세, 진행 스텝바만 있던 화면 — 기능을 홈 카드로 흡수 후 제거), `NewContractPage`(방 등록과 통합).

### 방 상세 화면이 없어진 이유 (설계 결정)
방을 눌러도 더 이상 별도 상세 화면으로 안 간다. 대신:
- 홈 카드 안에 4단계(메모/입실 링크/퇴실 링크/최종 보고서)가 **세로 진행선**으로 표시되고, 각 행을 누르면 해당 설정 페이지로 바로 이동
- 방 이름을 누르면 그 자리에서 바로 수정 가능 (`EditableName`, 확인/취소 버튼)
- 카드 오른쪽 `⋯` 버튼 → 하단 `ActionSheet` 메뉴: "방 이름·주소 수정", "새 입주자 시작하기", "방 삭제"

### 디자인 시스템 상수 (App.jsx 상단)
```js
C   // 색상: primary(#3366FF), success, warning, danger, gray 계열
R   // 둥근 반경: sm(10) md(12) lg(16) full(999)
F   // 폰트 크기: xs(11) sm(13) base(15) lg(17) xl(20) xxl(24)
```
새 UI를 만들 때 이 상수를 반드시 사용한다.

### 공용 컴포넌트 (App.jsx 상단)
`Page`, `NavBar`, `SCard`, `DataRow`, `PrimaryBtn`, `OutlineBtn`, `GhostBtn`,
`FixedBottom`, `Inp`, `Textarea`, `Toggle`, `ErrBox`, `InfoBanner`, `Empty`,
`StatCard`, `DatePicker`, `PhotoGrid`, `PhotoModal`,
`ConfirmModal`(브라우저 기본 confirm/alert 대체 — 앱 안 확인창, `askConfirm()` 헬퍼로 호출),
`ActionSheet`(하단에서 올라오는 메뉴), `EditableName`(터치해서 이름 수정), `RoomStatusRow`(세로 진행선 한 줄 행)

새 컴포넌트를 만들기 전에 이 목록을 먼저 확인한다.

**중요**: `window.confirm` / `window.alert`는 카카오 인앱브라우저 등에서 자동 취소되어 버튼이 먹통처럼 보이는 문제가 있었음 (2026-09-20 발견·수정). 앞으로 확인창이 필요하면 반드시 `ConfirmModal`/`askConfirm()`을 쓴다. 브라우저 기본 팝업은 절대 쓰지 않는다.

### 계약(contract) 상태 흐름
```
checkinSubmitted=false, checkoutSubmitted=false  (블랭크 계약, 링크는 이미 발급됨)
  → 임차인 입실 제출 → checkinSubmitted=true (이 시점에 baseline 스냅샷 저장, 아래 참고)
  → 임차인 퇴실 제출 → checkoutSubmitted=true, status:"submitted"
  → 임대인이 "계약 마무리하기" → status:"ended", 같은 방에 새 블랭크 계약 자동 생성
```
- **방을 만들면 계약도 즉시 자동 생성됨** (`blankProperty` + `blankContract`, App.jsx 상단). 별도로 "계약 시작" 버튼을 누를 필요 없음 — 처음 로그인 시에도 `ensureFirstRoom()`으로 "방 1"이 자동 생성됨
- "새 입주자 시작하기"(홈 카드 ⋯메뉴) 또는 계약 마무리 시 자동으로 이전 계약은 `ended`로, 새 블랭크 계약이 즉시 생성됨 → 항상 링크를 바로 발급할 수 있는 상태 유지

### 기준 사진(refPhotos) 저장 정책 (2026-09-21 구현)
- 방(`property`)의 `refPhotos`는 계약과 무관하게 방에 계속 남아있음 — 새 입주자를 시작해도 사진을 다시 올릴 필요 없이 재사용 가능 (입실 링크 설정 화면에 "이전에 올려 둔 사진이에요" 안내)
- **단, 임차인이 입실을 확인(checkinSubmitted)하는 순간, 그 시점의 `spaces`+`refPhotos`를 계약(`contract.baseline`)에 스냅샷으로 복사**한다. 이후 임대인이 방 사진을 바꿔도 이미 진행 중이거나 끝난 계약의 퇴실 폼/최종 보고서는 스냅샷(`co.baseline`)을 계속 사용하므로 지난 기록이 바뀌지 않음
- `co.baseline`이 없는(이 기능 이전에 만들어진) 계약은 방의 현재 `refPhotos`를 그대로 사용하는 fallback 유지

---

## 완료된 작업 (최근 순)

### ✅ 랜딩 페이지 전면 개편 (2026-09-21~22)
- 스토리 구성: 첫 화면(질문+보고서 미리보기) → "이미 보증금은 돌려줬는데 이런 상태라면?" → "임차인이라면 이런 사진 올릴 수 있을까요?"(냉장고 전/후) → **"이렇게 사용해요"**(입주할 때/퇴실할 때/최종 보고서 3단계, 실제 앱 캡처 동일 사진으로 스토리 이어짐) → 장점 4개(짧은 문구) → 이런 분께 좋아요(5개) → FAQ 4개 → 마지막 CTA
- 사용설명서 그림은 전부 headless Chrome(puppeteer-core)으로 실제 앱을 조작해 캡처한 것 (`public/landing/app-*.jpg`). 캡처 스크립트는 프로젝트 밖 scratchpad에 두고 저장소에는 올리지 않음
- 브라우저 기본 confirm/alert를 전부 `ConfirmModal`로 교체하며 발견된 버그였음 — 링크 복사 후 "저장됨" 피드백도 이때 추가

### ✅ 홈 화면 재구성 + 방 순서 변경 (2026-09-20~21)
- 방 카드 안에 진행 상태(메모→입실→퇴실→보고서) 세로선 표시, 링크 "보냄/확인함" 시각 기록
- 홈 상단 통계 카드를 "전체/입주중/공실"에서 "입실 확인 대기/퇴실 제출 대기/확인할 보고서"로 교체
- 방 카드 길게 누르기(0.5초) 또는 ⋯메뉴로 순서 변경 모드 진입 (드래그 또는 ▲▼ 버튼)
- 지난 보고서 목록 분리 + 개별 삭제 기능

### ✅ 개인정보/동의 (2026-09-21)
- 회원가입: 휴대폰번호(선택) + 필수/선택 동의 체크박스, 형식 검증
- 임차인 입실/퇴실 폼에 개인정보 안내 박스 추가

### ✅ 브라우저 뒤로가기 버그 (2026-06-22)
- `push()` → `history.pushState()` 동시 호출로 브라우저 히스토리 동기화
- `pop()` → `history.back()` 으로 변경, `popstate` 이벤트로 스택 감소
- 앱 진입 시 센티널 푸시 + 항상 재푸시 → 루트에서 실수로 앱 밖으로 나가는 것 방지
- 루트 화면에서 첫 뒤로가기 → "한 번 더 누르면 앱이 종료돼요" 토스트
- 2초 안에 두 번째 뒤로가기 → `window.close()` (카카오 인앱브라우저 WebView 닫힘)

---

## 남은 작업 (우선순위 순)

### 1. Supabase DB 연결 (다음 작업)
- **현재**: 모든 상태가 `useState` in-memory — 새로고침하면 전부 초기화
- `.env.local`에 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` 설정 필요
- Supabase 코드는 `src/lib/supabase.js`에 집중
- **필요 테이블**: users, properties, contracts, channels, logs, reviews
- 이때 같이 처리할 것:
  - 사진을 base64가 아니라 Storage에 업로드 + URL 참조로 전환 (지금은 `refPhotos`/`checkinData`/`checkoutData`에 base64 문자열을 그대로 넣고 있어 용량이 큼). 업로드 전 클라이언트에서 리사이즈(가로 1280px 안팎) 권장
  - 계약 `baseline` 스냅샷을 사진 파일 복사가 아니라 참조 방식으로 저장 (용량 절감)
  - 지난 보고서 보관 정책 결정 필요 (건수 제한보다 기간 기준 + 삭제 전 안내를 권장, 2026-09-21 논의)
  - 회원 탈퇴는 현재 자리표시자(실제로 데이터 안 지움) — 진짜 탈퇴 로직 필요

### 2. 정식 개인정보처리방침 문서 작성
- Supabase 연결(실제 데이터 저장 시작) 전에 준비
- 임차인 동의 방식을 체크박스형 명시적 동의로 강화할지 검토

### 3. PWA 설정
- `vite-plugin-pwa` 설치, `manifest.json` 한국어 앱 이름
- 홈 화면 추가 시 아이콘, 스플래시 스크린
- iOS Safari 지원 메타태그 확인

### 4. 소셜 로그인 실제 연동
- 현재 카카오/네이버 버튼은 UI만 있고 실제로 DEMO 계정으로 연결됨
- Supabase Auth로 카카오/네이버 OAuth 연동 필요

### 5. 카카오 알림톡 (사업자등록증 발급 후)
- 사업자등록증 + 비즈니스 채널 인증 필요, 템플릿 심사(2~5영업일)
- 용도: 임차인이 입실/퇴실 확인 제출했을 때 임대인에게 알림
- 임대인→임차인 링크 전달은 계속 수동(문자/카톡 복사-붙여넣기) 유지 — 자유 메시지라 알림톡 템플릿 심사에 안 맞음

### 6. 확인된 문제 해결 요청 대응 (필요해지면 추가, 2026-09-21 논의)
퇴실 보고서에 이상이 있을 때 지금은 전화로만 해결 가능. 필요해지면 이 순서로:
1. 보고서 화면에서 기존 메모(`ContractMemoPage`의 자유 메모)를 보이게/쓰게
2. 임대인이 직접 찍은 사진을 보고서에 추가
3. 임차인에게 추가 제출을 요청하는 기능 (원본은 수정 불가, 덧붙이기만)

### 7. (검토만 됨, 보류) 링크 자동 전송
문자/공유 API로 "문자로 보내기" 버튼을 만드는 안을 검토했으나 "일단 써보고 필요하면"으로 보류. 필요해지면 참고: 문자 앱을 여는 방식과 OS 공유 시트를 여는 방식 두 가지가 있음.

---

## UX 원칙

### 임대인 UX
- **홈 화면이 곧 컨트롤 타워**: 별도 방 상세 화면 없이 카드에서 모든 상태를 보고 바로 다음 행동으로 진입
- **진행 상태 항상 보임**: 카드 안 세로 진행선으로 현재 계약이 어느 단계인지 즉시 인지
- **다가오는 만료 먼저**: 홈 탭 상단에 30일 이내 만료 계약 타임라인 표시 (지난 계약은 "N일 지남"으로 표시)
- **링크 복사 = 핵심 액션**: 메시지 + 링크 한 번에 복사, 누르면 "보냄"으로 기록됨
- **파괴적 액션은 항상 확인창**: 삭제, 새 입주자 시작 등은 `ConfirmModal`로 확인 후 실행

### 임차인 UX (입실/퇴실 폼)
- **앱 설치 불필요**: URL 파라미터(`?token=...`)로 직접 폼 진입
- **단계별 안내**: 무엇을 해야 하는지 텍스트로 명확히 안내
- **오류는 자동 스크롤**: 빠뜨린 필드로 부드럽게 스크롤 이동 (`scrollIntoView`), alert 대신 화면 안 안내 문구 사용
- **한 번만 제출**: 이미 제출한 토큰 재접근 시 "이미 제출됐어요" 안내, 재제출 불가(근거 자료로서의 신뢰성 유지 목적)
- **개인정보 안내**: 제출 직전 어떤 목적으로 정보가 쓰이는지 안내 박스 표시

### 모바일 우선 규칙
- 기준 너비: `max-width: 480px` (index.css의 `#root`)
- 터치 타겟 최소 44px (버튼 padding 기준)
- 하단 고정 주요 버튼: `FixedBottom` 컴포넌트 사용
- 바텀시트 패턴: `DatePicker`, `ActionSheet`처럼 하단에서 올라오는 오버레이

---

## 코딩 규칙

### 핵심 원칙
- **기능 하나씩**: 한 번에 하나의 기능만 수정. 관련 없는 코드는 건드리지 않는다
- **설명은 한국어**: 모든 질문, 제안, 변경 설명을 한국어로 한다
- **기존 패턴 먼저**: 공용 컴포넌트와 디자인 상수(C, R, F)를 재사용한다
- **UI 변경은 코드 수정 전 합의**: 새 화면/문구/구조를 바꿀 때는 먼저 방향을 텍스트로 제안하고 사용자 확인을 받은 뒤 코드를 수정한다 (2026-09-19 사용자 요청)
- **UI 변경은 브라우저로 직접 검증**: 코드만 보고 끝내지 말고 preview_start로 띄운 뒤 실제로 클릭해서 확인한다

### 파일 구조 (현재 → 목표)
```
src/
  App.jsx          # 현재 모든 코드가 여기 (약 1980줄)
  main.jsx
  index.css
  lib/
    supabase.js    # Supabase 클라이언트 (연결 후 생성)
  components/      # 공용 컴포넌트 분리 시 여기로
  pages/           # 페이지 컴포넌트 분리 시 여기로
public/
  landing/         # 랜딩용 이미지 (실제 앱 캡처 + 방 사진, 저장소에 포함)
landing-assets/     # 랜딩용 원본 사진 (.gitignore 처리, 저장소에 없음)
```
지금은 App.jsx 단일 파일 유지. 분리는 Supabase 연결 이후에 논의.

### 데이터 변경 방법
- `updateDb(s => ({...s, ...}))` — 클로저 안전, 비동기 배치 처리
- `upDb({key: value})` — 최상위 키만 바꿀 때 단순 패치
- `useState` 직접 조작 금지 — 반드시 위 두 함수 경유
- 확인이 필요한 액션은 `askConfirm(message, onOk, {okLabel, danger})` 사용 (App() 컴포넌트 안에 정의됨)

### 인증·권한
- `user.isAdmin === true` 이면 Admin 탭 표시
- 데모 계정: `DEMO = { id:"demo", email:"demo@test.com", pw:"1234", isAdmin:true }`
- 임차인(토큰 사용자)은 계정 없이 접근 — `linkToken` 상태로 처리

### 환경변수 (.env.local, git 제외)
```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

---

## 개발 명령어

```bash
npm run dev      # 개발 서버 (http://localhost:5173)
npm run build    # 프로덕션 빌드
npm run preview  # 빌드 결과 미리보기
npm run lint     # ESLint 검사
```

## 배포 (용어: 커밋 → 푸시 → 배포)
```bash
git add <파일들>          # 원하는 파일만 골라서 스테이징 (add -A 금지)
git commit -m "..."       # 로컬에 저장 지점 생성
git push origin main      # GitHub에 업로드 → Vercel이 자동으로 감지해 배포 (20~30초 소요)
```
- Vercel 프로덕션 주소: https://ipteosil.vercel.app
- `.claude/settings.local.json` 등 개인 설정 파일은 커밋하지 않는다
